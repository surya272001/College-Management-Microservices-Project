server:
  port: 8372

eureka:
  instance:
    hostname: localhost
  client:
    registrationWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/