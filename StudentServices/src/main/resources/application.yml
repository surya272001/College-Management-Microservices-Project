eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8372/eureka/

server:
  port: 9099
spring:
  application:
    name: Student-Services
  dataSource:
    url: jdbc:mysql://localhost:3306/college
    username: root
    password: pass@word1
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
